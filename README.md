# Gempo Desk Prototype

現場ポケット風の現場管理プロトタイプです。

## ローカル確認

`index.html` をブラウザで開くと確認できます。

## デプロイ

静的サイトとして公開できます。

- Vercel: `vercel --prod`
- Netlify: `netlify deploy --prod --dir=dist`

ビルド成果物は `npm run build` で `dist/` に作成されます。
